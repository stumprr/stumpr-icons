import { chartIconNode } from "@stumpr/icons";
import { createStumprIcon, type StumprIcon } from "../createStumprIcon";

export const ChartIcon: StumprIcon = createStumprIcon("ChartIcon", chartIconNode);
