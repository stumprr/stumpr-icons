import { commandIconNode } from "@stumpr/icons";
import { createStumprIcon, type StumprIcon } from "../createStumprIcon";

export const CommandIcon: StumprIcon = createStumprIcon("CommandIcon", commandIconNode);
