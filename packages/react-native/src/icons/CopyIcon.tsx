import { copyIconNode } from "@stumpr/icons";
import { createStumprIcon, type StumprIcon } from "../createStumprIcon";

export const CopyIcon: StumprIcon = createStumprIcon("CopyIcon", copyIconNode);
